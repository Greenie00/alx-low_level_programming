#include "main.h"

/**
 * _islower - Returns 1 if small alphabet is found else returns 0
 *
 * Result: 0 and 1
 */

int _islower(int c)
{

	char alphab;

	if (alphab = 'a' && alphab <= 'z')
		return (1);
	else
		return (0);

	return (0);
}
